# Copie para .env e ajuste se necessário: cp .env.example .env
# Estes valores são de DESENVOLVIMENTO. Nunca versione credenciais reais.

APP_NAME=decada
APP_ENV=dev

POSTGRES_USER=decada
POSTGRES_PASSWORD=decada
POSTGRES_DB=decada
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
